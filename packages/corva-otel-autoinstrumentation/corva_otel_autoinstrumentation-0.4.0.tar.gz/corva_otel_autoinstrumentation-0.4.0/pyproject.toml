[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "corva-otel-autoinstrumentation"
version = "0.4.0"
description = "OpenTelemetry autoinstrumentation for Python at corva.ai"
readme = "README.md"
requires-python = ">=3.6"
authors = [
    { name = "Radoslav Kirilov", email = "radoslav.kirilov@corva.ai" },
    { name = "Ivaylo Papratilov", email = "ivaylo.papratilov@corva.ai" }
]
dependencies = [
    "opentelemetry-instrumentation"
]
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent"
]

[project.urls]
"Homepage" = "https://github.com/corva-ai/otel/tree/main/python/autoinstrumentation"

[tool.hatch.envs.default]
dependencies = [
    "pytest",
    "flake8"
]

[tool.hatch.build.targets.sdist]
include = [
  "src/corva_otel",
]

[tool.hatch.build.targets.wheel]
packages=["src/corva_otel"]
