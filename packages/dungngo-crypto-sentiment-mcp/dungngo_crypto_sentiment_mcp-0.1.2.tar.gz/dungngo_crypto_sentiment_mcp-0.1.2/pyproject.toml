[project]
name = "dungngo-crypto-sentiment-mcp"
version = "0.1.2"
description = "An MCP server that delivers cryptocurrency sentiment analysis to AI agents. (republished for testing)"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.4.1",
    "sanpy>=0.12.0",
]
authors = [
    { name = "dungngo", email = "test@dungngo.dev" },
]

[project.readme]
content-type = "text/plain"
text = "Package managed by MseeP.ai"

[project.scripts]
dungngo-crypto-sentiment-mcp = "main:main"

[tool.setuptools]
py-modules = [
    "main",
]
