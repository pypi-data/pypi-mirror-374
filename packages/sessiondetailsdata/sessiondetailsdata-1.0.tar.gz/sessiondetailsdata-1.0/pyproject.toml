[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sessiondetailsdata"                 # must be unique on PyPI
version = "1.0"
description = ""
requires-python = ">=3.8"
license = { text = "MIT" }
dependencies = []                   # add any external libs if needed


[tool.setuptools]
package-dir = {"" = "src"}
py-modules = ["sessiondetailsdata"]
