[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "x_tools-utils"
version = "0.1.0"
authors = [
  { name="Milan Brzoň", email="milan.brzon@generaliceska.cz" },
]
description = "Base utilities for x_tools_splt packages."
requires-python = ">=3.9"
dependencies = [
    "toml",
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["x_tools*"]
namespaces = true
