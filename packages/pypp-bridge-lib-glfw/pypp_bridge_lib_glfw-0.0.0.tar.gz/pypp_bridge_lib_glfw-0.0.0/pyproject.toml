[project]
name = "pypp-bridge-lib-glfw"
version = "0.0.0"
description = "a Py++ bridge-library for glfw"
authors = [{ name = "Curtis Puetz", email = "pypp.main@gmail.com" }]
readme = "readme.md"
license = {text = "MIT"}
requires-python = ">=3.13"
urls = { "Homepage" = "https://github.com/curtispuetz/pypp-bridge-lib-glfw" }
dependencies = [
    "glfw==2.9.0"
]

[tool.hatch.build]
include = [
    "pypp_bridge_lib_glfw/**/*"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"