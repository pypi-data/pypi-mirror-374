[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "flexAgent"
version = "0.1.0"
description = "Um orquestrador de agentes flexível"
authors = [{ name = "fernando", email = "fernando.santos@passabot.com" }]
license = "MIT"
readme = "README.md"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["flexAgent"]

