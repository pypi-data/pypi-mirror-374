""" The processing context defines how data instances' values (the data entities) can be processed, transformed, using transformations sequences."""
from Client.context import Context


class ProcessingContext(Context):
    """The processingContext class, will its relevant evidences.

    Args:
        Context (Context): The base class of Processing context.
    """