# SPDX-FileCopyrightText: 2025 Laurent Modolo <laurent@modolo.fr>
#
# SPDX-License-Identifier: AGPL-3.0-or-later

[project]
name = "midr"
version = "2.0.1"
description = "Compute the irreproducible discovery rate (IDR) for a given dataset."
readme = "README.md"
requires-python = ">=3.13"
classifiers = [
    # How mature is this project? Common values are
    #   3 - Alpha
    #   4 - Beta
    #   5 - Production/Stable
    "Development Status :: 4 - Beta",

    # Environment
    "Operating System :: MacOS",
    "Operating System :: POSIX :: Linux",
    "Environment :: GPU",

    # Indicate who your project is intended for
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering :: Information Analysis",
    "Topic :: Scientific/Engineering :: Bio-Informatics",

    # Specify the Python versions you support here.
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
]
dependencies = [
    "numpy>=2.2.3",
    "polars-lts-cpu>=1.31.0",
    "pyarrow>=19.0.1",
    "rich-click>=1.8.9",
    "torch>=2.6.0",
]
license = "AGPL-3.0-or-later"
license-files = ["LICEN[CS]E*"]

[dependency-groups]
dev = [
    "plotnine>=0.14.5",
    "pytest>=8.3.5",
    "pytest-cov>=6.2.1",
    "matplotlib>=3.10.1",
    "twine>=6.1.0",
]

[project.urls]
Homepage = "https://gitbio.ens-lyon.fr/LBMC/physbio/idrpy"

[project.scripts]
midr = "midr.main:main"

[tool.setuptools]
packages = ["midr"]

[tool.pyright]
venvPath = "."
venv = ".venv"


[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[tool.uv.workspace]
exclude = ["extra/"]
