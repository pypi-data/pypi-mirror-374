[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "brotli-python"
version = "1.9.12"
description = "Dummy brotli-python package for PyPI"
authors = [{name="Your Name", email="you@example.com"}]
readme = "README.md"
requires-python = ">=3.7"

[tool.setuptools.packages.find]
where = ["src"]
