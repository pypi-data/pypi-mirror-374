[project]
name = "deploy-quack"
version = "0.1.0"
description = "An awesome tool to deploy applications with docker swarm -- COMING SOON"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
quack = "deploy_quack.cli:main"
deploy-quack = "deploy_quack.cli:main"

[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"