apiVersion: v1
kind: Service
metadata:
  name: mlflow-deployment
  namespace: intellithing
  labels:
    app: mlflow
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: mlflow
  type: ClusterIP
