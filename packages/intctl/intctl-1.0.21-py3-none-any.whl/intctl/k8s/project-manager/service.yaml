apiVersion: v1
kind: Service
metadata: 
  name: project-manager-deployment
  namespace: intellithing
  labels:
    app: project-manager
    proxy-managed: "true"  
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: project-manager
  type: ClusterIP
