apiVersion: v1
kind: Service
metadata: 
  name: gitea-manager-deployment
  namespace: intellithing
  labels:
    app: gitea-manager
    proxy-managed: "true"  
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: gitea-manager
  type: ClusterIP
 