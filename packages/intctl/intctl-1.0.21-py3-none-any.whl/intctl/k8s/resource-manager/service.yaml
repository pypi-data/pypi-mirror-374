apiVersion: v1
kind: Service
metadata:
  name: resource-manager-deployment
  namespace: intellithing
  labels:
    app: resource-manager
    proxy-managed: "true"
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: resource-manager
  type: ClusterIP
