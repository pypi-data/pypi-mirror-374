apiVersion: v1
kind: Service
metadata: 
  name: workflow-deployment
  namespace: intellithing
  labels:
    app: workflow
    proxy-managed: "true"  
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: workflow
  type: ClusterIP

