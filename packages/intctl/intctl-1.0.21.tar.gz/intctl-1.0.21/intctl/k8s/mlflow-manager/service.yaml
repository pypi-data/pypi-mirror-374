apiVersion: v1
kind: Service
metadata: 
  name: mlflow-manager-deployment
  namespace: intellithing
  labels:
    app: mlflow-manager
    proxy-managed: "true"  
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: mlflow-manager
  type: ClusterIP

