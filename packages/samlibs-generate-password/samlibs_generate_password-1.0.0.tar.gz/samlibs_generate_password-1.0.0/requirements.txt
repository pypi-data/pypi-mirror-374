# No runtime dependencies - keeping it ultra lightweight!
# Development dependencies (for building and testing only)

# Build tools
build>=0.8.0
twine>=4.0.0

# Optional: Testing framework (if you want to use pytest instead of built-in tests)
# pytest>=7.0.0