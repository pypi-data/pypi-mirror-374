<tal:if define="output view.view.input_form.request.annotations.get('form.output')">
    <div>${structure:view.submit_message}</div>
    <div tal:condition="output">
		${structure:'<br />'.join(output)}
	</div>
</tal:if>