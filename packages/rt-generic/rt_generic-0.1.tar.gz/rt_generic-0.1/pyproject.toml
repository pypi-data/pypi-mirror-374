[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "rt-generic"
description = "tools to make typing more consistent in older versions of python3, and access generic variables at runtime"
authors = [{name="Doug Sojourner", email="doug@sojournings.org"}, ] # email=failing
readme = "README.md"
requires-python = ">=3.7,<4.0"
license = { text = "GNU General Public License v3 (GPLv3)" }
version = "0.1"

dependencies = ['typing_extensions; python_version < "3.11"', ]

classifiers=[
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
	"License :: OSI Approved :: GNU General Public License v3 (GPLv3)",
	"Natural Language :: English",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
	"Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.7",
    "Programming Language :: Python :: 3.8", 
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
	"Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Software Development :: Libraries",
    "Topic :: Software Development",
	"Typing :: Typed",
]

keywords = ["runtime", "generic", "typed", "typing", "versions"]

[dependency-groups]
all = [
	"virtualenv",
	]
test = [
	"mypy>=0.942",
	"pytest>=7.4.4",
	"black>=24.4.0; python_version >= '3.8'",
	"black; python_version < '3.8'",
	]
build = [
	  "hatchling.build",
	  ]
local-source = ["simple503", "simpleindex", ]
self = ["rt-generic==0.1"]

[project.urls]
repository = "https://codeberg.org/Sojournings/rt-generic.git"

[tool.poetry]
include = [
    { path = "tests/rt_generic_tests.py", format = "sdist" },
    { path = "rt-generic", format = ["sdist", "wheel"] },
]

[tool.mypy]
ignore_missing_imports = false
check_untyped_defs = true
#ignore_without_code = true
#warn_unused_ignore = true

# siginificantly speed up pytest
[tool.pytest.ini_options]
testpaths = [ "tests"]

[tool.black]
