repos:
- repo: local
  hooks:
    # Run the linter via Pixi
    - id: pixi-lint
      name: pixi lint
      entry: pixi run lint
      language: system
    # Run the formatter via Pixi
    - id: pixi-format
      name: pixi format
      entry: pixi run format
      language: system
