from setuptools import setup, find_packages

setup(
    name='sscreen',
    version='0.1',
    packages=find_packages(),
    install_requires=['PyQt6'],
    description='A modern PyQt6-based GUI library',
    author='Sanki',
)
