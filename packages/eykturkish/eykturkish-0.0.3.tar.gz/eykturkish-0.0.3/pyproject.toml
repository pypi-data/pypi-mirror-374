[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "eykturkish"
version = "0.0.3"
description = "Türkçe metin işleme veya yardımcı Python kütüphanesi."
requires-python = ">=3.8"
keywords = ["turkish", "language", "nlp", "eyk", "utility"]
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
dependencies = [

]
license = { file = "LICENSE.md" }