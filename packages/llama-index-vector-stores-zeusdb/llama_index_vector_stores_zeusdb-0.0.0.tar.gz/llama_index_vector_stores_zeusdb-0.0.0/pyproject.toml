[project]
name = "llama-index-vector-stores-zeusdb"
version = "0.0.0"
description = "Advanced RAG made simple: LlamaIndex document processing + ZeusDB enterprise vector storage with production-ready performance."

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"