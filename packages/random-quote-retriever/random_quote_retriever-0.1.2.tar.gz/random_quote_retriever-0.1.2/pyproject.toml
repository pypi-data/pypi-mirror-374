[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "random-quote-retriever"
version = "0.1.2"
description = "A simple CLI tool to fetch random quotes from ZenQuotes API"
authors = [
  { name = "Muhammad Hasan", email = "mhasanali210@google.com" }
]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.9"

dependencies = [
  "requests>=2.32.3"
]

[project.scripts]
quote = "main:main"
