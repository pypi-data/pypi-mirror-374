[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "physical_spline"
version = "0.1.0"
description = "Physical spline designer and evaluator"
authors = [{name = "Jonas Torzewski"}]
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "numpy",
    "scipy",
]