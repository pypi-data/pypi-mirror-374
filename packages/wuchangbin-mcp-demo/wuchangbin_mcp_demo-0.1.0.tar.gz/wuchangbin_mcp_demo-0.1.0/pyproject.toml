[project]
name = "wuchangbin-mcp-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "binzi", email = "15830091676@163.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.13.1",
]

[project.scripts]
wuchangbin-mcp-demo = "wuchangbin_mcp_demo:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
