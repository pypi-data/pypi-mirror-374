{
  "name": "self-organizing-maps",
  "root": "packages/self_organizing_maps", 
  "projectType": "library",
  "sourceRoot": "packages/self_organizing_maps/self_organizing_maps",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "python -m pip install build",
          "python -m build"
        ],
        "cwd": "packages/self_organizing_maps"
      }
    },
    "test": {
      "executor": "nx:run-commands", 
      "options": {
        "commands": [
          "python -m pytest tests/ -v"
        ],
        "cwd": "packages/self_organizing_maps"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "python -m ruff check .",
          "python -m black --check ."
        ],
        "cwd": "packages/self_organizing_maps"
      }
    }
  },
  "tags": ["type:package", "scope:research", "domain:clustering"]
}