- hosts: '{{ hosts_name }}'
  name: install ha
  tasks:
    - name: create user
      create_user:
        group: "{{ group }}"
        user: "{{ user }}"

    - name: install ha
      import_tasks: task_ha.yml

