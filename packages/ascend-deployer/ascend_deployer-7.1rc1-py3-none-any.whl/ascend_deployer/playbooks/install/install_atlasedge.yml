- hosts: '{{ hosts_name }}'
  name: install atlasedge
  tasks:
    - name: create user
      create_user:
        group: "{{ group }}"
        user: "{{ user }}"

    - name: install atlasedge
      import_tasks: task_atlasedge.yml
