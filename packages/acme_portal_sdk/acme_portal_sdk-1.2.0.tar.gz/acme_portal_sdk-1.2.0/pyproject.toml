[build-system]
requires = ["setuptools>=46.4.0", "wheel>=0.36.2"]
build-backend = "setuptools.build_meta"

[project]
name = "acme_portal_sdk"
version = "1.2.0"
description = "acme-portal-sdk helps to manage deployments for applications that implement \"flows\" (Jobs/DAGs/Workflows). It's intended to be used with acme-portal VSCode extension."
readme = "README.md"
classifiers = [
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]
requires-python = ">=3.9"
dependencies = ["python-dotenv"]

[[project.authors]]
email = "guner.stan@gmail.com"

[project.scripts]
aps = "acme_portal_sdk._main:main"
aps-prefect-deploy = "acme_portal_sdk.prefect.prefect_deploy:main"

[project.urls]
homepage = "https://blackwhitehere.github.io/acme-portal-sdk/"
repository = "https://github.com/blackwhitehere/acme-portal-sdk"

[project.optional-dependencies]
dev = [
    "pytest",               # for testing
    "pytest-cov",           # for test coverage
    "uv",                   # for venv
    "mkdocs",               # for documentation
    "mkdocs-material",      # theme for mkdocs
    "mkdocstrings[python]", # mkdocs plugin to generate API docs from sources
    "jupyterlab",           # for jupyter notebooks
    "isort",                # for import sorting
    "ruff",                 # for linting
    "pre-commit",           # for pre-commit hooks
]
prefect = [
    "prefect",
    "acme-config>=0.0.14",
]
airflow = ["apache-airflow>=3.0.0", "requests"]

[tool.setuptools]
package-dir = { "" = "src" }
packages = { find = { where = ["src"] } }
# https://github.com/astral-sh/uv/issues/9513#issuecomment-2519527822
license-files = []
# [tool.setuptools.package-data]
