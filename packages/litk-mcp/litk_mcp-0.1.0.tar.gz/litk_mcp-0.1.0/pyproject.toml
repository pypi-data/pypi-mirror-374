[project]
name = "litk-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.13.1",
]

[project.scripts]
litk-mcp = "litk_mcp:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
