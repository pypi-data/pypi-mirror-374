[project]
name = "sc-orchestrator"
version = "0.2.19"
description = "Szymanski Consulting – Orchestrator for PM/vCISO/Quoting"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["PyYAML>=6.0.2", "pytz>=2024.1", "requests>=2.32.3"]

[project.scripts]
sc-orchestrate = "sc_orchestrator.cli:main"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
