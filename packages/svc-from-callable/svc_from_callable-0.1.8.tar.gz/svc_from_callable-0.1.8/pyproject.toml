[tool.poetry]
name = "svc-from-callable"
version = "0.1.8"
description = ""
authors = ["Your Name <you@example.com>"]
packages = [{ include = "svc_from_callable" }]
readme = "README.md"


[tool.poetry.dependencies]
python = "^3.10"
sw-product-lib = "^0.9.12"
uvicorn = "^0.23.2"

[tool.poetry.group.dev.dependencies]
pytest = "^7.4.3"
httpx = "^0.25.0"
black = "^23.10.1"
isort = "^5.12.0"
flake8 = "^6.1.0"
pre-commit = "^3.5.0"
python-dotenv = "^1.0.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
