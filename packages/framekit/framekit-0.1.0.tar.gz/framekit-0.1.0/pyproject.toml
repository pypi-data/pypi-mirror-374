[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "framekit"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "numpy",
    "opencv-python",
    "PyOpenGL",
    "Pillow",
    "tqdm",
    "pygame",
    "mutagen",
    "librosa",
]

[tool.setuptools]
packages = ["framekit"]