import random

# Constants for the last characters/digits
last_lower_char = 'z'
last_upper_char = 'Z'
last_digit_char = '9'

# Random guesses for the last digits of pi and e (correct 1/10 on average)
last_pi_digit = random.randint(0, 9)
last_e_digit = random.randint(0, 9)
