[project]
name = "uv-publish-github"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "danielsyahputra", email = "daniel.purba@dana.id" }
]
requires-python = ">=3.11"
dependencies = [
    "requests>=2.32.5",
]

[project.scripts]
uv-publish-github = "uv_publish_github:main"

[build-system]
requires = ["uv_build>=0.8.14,<0.9.0"]
build-backend = "uv_build"
