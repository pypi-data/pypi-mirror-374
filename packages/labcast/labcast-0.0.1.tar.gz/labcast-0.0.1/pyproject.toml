# --------------------------------------------------------------------------------------------------
# SPDX-License-Identifier: Apache-2.0
# Copyright (C) 2025 Jayesh Badwaik <j.badwaik@fz-juelich.de>
# --------------------------------------------------------------------------------------------------
[project]
name = "labcast"
dynamic = ["version"]
description = 'A GitLab-to-GitLab CI connector'
readme = "readme.md"
license = "Apache-2.0"
authors = [
{ name = "Jayesh Badwaik", email = "jayesh@ambhora.com" },
]
dependencies = [
  "pyyaml",
  "ruamel.yaml>=0.18",
  "semver",
  "livereload"
]

[project.urls]
Source = "https://gitlab.com/ambhora/software/labcast"

[project.scripts]
labcast = "labcast.cli:main"

# --------------------------------------------------------------------------------------------------
# Hatch configuration
# --------------------------------------------------------------------------------------------------
[tool.hatch.version]
path = "src/labcast/metadata.py"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/labcast"]

[tool.hatch.build.targets.wheel.shared-data]
"share"="share"

# --------------------------------------------------------------------------------------------------
# PyTest configuration
# --------------------------------------------------------------------------------------------------
[tool.pytest.ini_options]
pythonpath = ["src"]
addopts = "--cov --cov-report term-missing --cov-fail-under=10"

[tool.black]
line-length = 100

[tool.hatch.envs.test]
dependencies = [
  "pytest>=6.3",
  "pytest-cov"
]


[tool.hatch.envs.mypy]
dependencies = [
  "types-PyYAML",
  "mypy",
  "semver",
  "pytest",
]

[tool.hatch.envs.docs]
dependencies = [
  "mkdocs-material",
  "mkdocstrings[python]",
  "mkdocs-material",
  "mkdocs-awesome-nav",
  "mkdocs-add-number-plugin"
]
