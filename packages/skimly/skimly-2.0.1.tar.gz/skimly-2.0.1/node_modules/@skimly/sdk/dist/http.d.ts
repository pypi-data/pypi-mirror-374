export interface HttpOptions {
    base: string;
    key: string;
    timeoutMs?: number;
    retries?: number;
}
export declare function postJSON<T>(path: string, payload: unknown, opts: HttpOptions): Promise<T>;
