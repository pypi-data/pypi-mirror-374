from setuptools import setup, find_packages

setup(
    name="caskada",
    version="0.0.1",
    packages=find_packages(),
    description="Initial placeholder for the caskada package.",
    author="zvictor",
)